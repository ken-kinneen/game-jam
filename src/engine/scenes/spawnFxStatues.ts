import type { Scene } from '@babylonjs/core';

/** Demo FX statues — no-op stub after Babylon migration (Phaser preFX removed). */
export function spawnFxStatues(_scene: Scene): void {
  // Intentionally empty: demo FX gallery was Phaser-preFX specific.
}
